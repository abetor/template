# CLAUDE.md

Thin adapter. `AGENTS.md` beside this file is the canonical entry point; read it first.
Then read `docs/STATUS.md` and `docs/tasks.md`. Run `python3 -m pytest -q` before and after a
change.
