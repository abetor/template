"""Vendored code from tool-template, copied into and owned by this repository.

Share contracts rather than imports: generated tools have no runtime dependency on the
template or sibling repositories. A local change is an intentional vendored-code fork whose
drift is detected by ``python3 -m tooltemplate diff``.
"""
