# Extensions

Add each new capability as a directory here with its own README. Extensions may import the
core. The core must not depend on extension internals; only command registration belongs in
`cli.py`.
